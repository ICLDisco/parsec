SET(DAGUE_PROFILING_INCLUDE_DIR "${CMAKE_SOURCE_DIR}/dague/")
SET(DAGUE_PROFILING_LIB_DIR "${CMAKE_BUILD_DIR}/dague/")
SET(DAGUE_PROFILING_LIBRARIES "dague;dague-base")

LINK_DIRECTORIES("${DAGUE_PROFILING_LIB_DIR}")
INCLUDE_DIRECTORIES("${DAGUE_PROFILING_INCLUDE_DIR}")

add_executable(sp-demo sp-demo.c)
target_link_libraries (sp-demo "${DAGUE_PROFILING_LIBRARIES}")
